<% content_for :breadcrumb do %>
  <div class="flex items-center">
    <%= breadcrumbs ["Home",root_path], ["Dashboard", root_path], ['Cultivation Batches', cultivation_batches_path], "Locations & Tasks" %>
  </div>
<% end %>

<div class="pa4">
  <div class="flex flex-column justify-between bg-white box--shadow">
    <div class="pa4 min-h-600">
      <% if params[:step].present? %>
        <%= react_component 'cultivation/batches_setup/BatchLocationApp',
          {
            batchId: @record.id.to_s,
            batchSource: @record.batch_source,
            plantType: @plant_type,
            locations: @locations
          } %>
      <% else %>
        <div class="fl w-100 flex flex-column">
          <%= react_component 'cultivation/tasks_setup/TaskSetup', {batch_id: @record.id, batch: @batch_attributes} %>
        </div>
      <% end %>
    </div>
  </div>
</div>
