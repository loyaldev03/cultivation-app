toast('All rooms has been deleted', 'notice');
$_("#rooms_grid").innerHTML = ("<%= j render 'rooms_list',
                               facility_id: @rooms_info_form.facility_id,
                               rooms: [] %>")
<%= render 'rebind_carousels', goto: 0 %>
