var roomsGridElm = $_("#rooms_grid")
if (roomsGridElm) {
  roomsGridElm.innerHTML = ("<%= j render 'rooms_list',
                            facility_id: @rooms_info_form.facility_id,
                            rooms: @rooms_info_form.rooms %>")
}

<%= render 'rebind_carousels', goto: @mode == 'increment' ? -1 : 0 %>
