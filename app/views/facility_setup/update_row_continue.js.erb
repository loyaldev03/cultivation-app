toast('Row Saved', 'success');
$_(".slide-panel__body").innerHTML = ("<%= j render 'row_shelves_trays_form',
                                      facility_id: @rows_form.facility_id,
                                      room_id: @rows_form.room_id,
                                      rows: @rows_form.rows %>")
