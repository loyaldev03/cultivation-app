toast('Shelf Saved', 'success');
$_("#rows_grid").innerHTML = ("<%= j render 'rows_list',
                                facility_id: @rows_form.facility_id,
                                room_id: @rows_form.room_id
                               %>")
<%= render 'row_shelf_trays' %>
<%= render 'carousel_reload' %>

<% if @row_shelves_trays_form.next_shelf_index == -1 %>
    show_row_setup_success("<%= @row_shelves_trays_form.id %>")
<% end %>

