toast('Shelf Saved', 'success');

$_('<%= ".carousel__card--#{@row_info_form.id}" %>').innerHTML = ("<%= j render 'row_card_body', row: @row_info_form %>")

<%= render 'row_shelf_trays' %>

<% if @row_shelves_trays_form.current_shelf_index == -1 %>
    hideSlidePanel();
    showRowSetupSuccess("<%= @row_shelves_trays_form.id %>")
<% end %>

