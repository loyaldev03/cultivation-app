<%if defined?(message)%>
  console.log('enter here');
  toast('<%=message%>');
<%end%>
$_("#right-sidebar").addEventListener("transitionend", function(event) {
  window.location.reload();
});

var ev = new MouseEvent('click');
$_("#hide-sidebar").dispatchEvent(ev);
