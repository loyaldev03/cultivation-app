<div class="ph4 pv3 bb b--light-grey">
  <h5 class="h6--font dark-grey ma0 ttc">Raw Materials</h5>
</div>

<div class="ph4 mt3 mb3 bt2">
  <label class="f5"><%= @raw_material.label %> Subcategory</label>

  <div class="mt2">
  <% @children.each do |child|%>
      <div class="flex items-center mb2">
        <% if child.is_active? %>
          <input class="mr2" type="checkbox" id="<%= child.id%>" value="<%= child.id %>" name="raw_material[ids][]" checked>
        <% else %>
          <input class="mr2" type="checkbox" id="<%= child.id%>" value="<%= child.id %>" name="raw_material[ids][]">
        <% end %>
        <label class="pointer lh-copy"><%= child.label %></label>
      </div>
  <% end %>
  </div>
</div>

